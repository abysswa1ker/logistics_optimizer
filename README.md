# Оптимізація логістичної мережі - MVP v1.0

Система оптимізації логістичної мережі з використанням двох алгоритмів: МПО (метод покоординатного спуску) та ЕМ-ГА (еволюційно-модифікований генетичний алгоритм).

## 🚀 Можливості

### Алгоритми оптимізації
- ✅ **МПО** - Метод покоординатного спуску (точний, детермінований)
- ✅ **ЕМ-ГА** - Генетичний алгоритм (швидкий, глобальний пошук)
- ✅ **Режим порівняння** - Автоматичне порівняння обох методів

### Функціональність
- ✅ Завантаження даних з CSV файлів
- ✅ Обчислення витрат мережі (фіксовані, обробка, транспортування)
- ✅ Оптимізація координат терміналів
- ✅ Автоматичне вимикання невигідних терміналів
- ✅ Експорт результатів у CSV для аналізу
- ✅ Генератор тестових мереж різних розмірів
- ✅ Пакетне тестування на множині мереж

### Візуалізація
- ✅ Візуалізація мережі до/після оптимізації
- ✅ Графіки порівняння витрат
- ✅ Порівняння методів оптимізації
- ✅ Покращена візуалізація неактивних терміналів

## 📋 Вимоги

- Python 3.8+
- matplotlib

## 🔧 Встановлення

### 1. Клонування репозиторію

```bash
git clone https://github.com/abysswa1ker/logistics_optimizer.git
cd logistics_optimizer
```

### 2. Встановлення залежностей

**Windows:**
```bash
pip install -r requirements.txt
```

**Linux/Mac:**
```bash
pip3 install -r requirements.txt
```

Або встановіть вручну:
```bash
pip install matplotlib
```

## 🎯 Використання

### Запуск оптимізації

```bash
python main.py
```

Програма покаже меню:

```
============================================================
ПРОГРАМА ОПТИМІЗАЦІЇ ЛОГІСТИЧНОЇ МЕРЕЖІ - MVP
============================================================

Доступні файли:
1. network_n15.csv
2. network_n20.csv
...
15. Вихід

Оберіть файл (1-15): 1

============================================================
ВИБЕРІТЬ РЕЖИМ ОПТИМІЗАЦІЇ
============================================================
1. Тільки МПО (Метод покоординатного спуску)
2. Тільки ЕМ-ГА (Еволюційний метод - генетичний алгоритм)
3. Порівняння обох методів
4. Вихід
============================================================

Оберіть режим (1-4): 3
```

### Генерація тестових мереж

```bash
python utils/network_generator.py
```

Створює 6 тестових мереж (n=15, 20, 25, 30, 35, 40 споживачів).

### Пакетне тестування

```bash
python utils/batch_test.py
```

Запускає автоматичне тестування обох методів на всіх мережах і створює CSV з результатами.

### Результати

Після запуску створяться:
- `results/{filename}_network_before.png` - мережа до оптимізації
- `results/{filename}_network_after.png` - мережа після оптимізації (МПО або ГА)
- `results/{filename}_comparison.png` - порівняння витрат
- `results/{filename}_methods_comparison.png` - порівняння МПО та ГА (режим порівняння)
- `results/{filename}_optimization_results.csv` - результати в CSV
- `results/batch_tests/performance_comparison.csv` - результати пакетного тестування

## 📊 Приклад результатів

### Режим МПО

```
============================================================
РЕЗУЛЬТАТИ ОПТИМІЗАЦІЇ
============================================================

Початкові витрати:     66,486.31
Фінальні витрати:      52,600.48
────────────────────────────────
Економія:              13,885.83 (20.89%)
Ітерацій:              2
============================================================
```

### Режим порівняння

```
╔══════════════════════════════════════════════════════════════════╗
║                ПОРІВНЯЛЬНИЙ АНАЛІЗ МЕТОДІВ                       ║
╚══════════════════════════════════════════════════════════════════╝

┌────────────────────┬──────────────┬──────────────┬──────────────┐
│ Параметр           │ МПО          │ ЕМ-ГА        │ Переможець   │
├────────────────────┼──────────────┼──────────────┼──────────────┤
│ Кінцева вартість   │ 52,600.48    │ 51,697.71    │ ЕМ-ГА ✓      │
│ Покращення (%)     │ 20.89%       │ 22.24%       │ ЕМ-ГА ✓      │
│ Час виконання      │ 0.09 с       │ 0.19 с       │ МПО ✓        │
│ Активних терміналів│ 3            │ 3            │ Однаково     │
├────────────────────┴──────────────┴──────────────┴──────────────┤
│ ВИСНОВОК: ЕМ-ГА знайшов кращий результат за 0.1 с                │
└──────────────────────────────────────────────────────────────────┘
```

## 📂 Структура проекту

```
logistics_optimizer/
├── data/                          # Вхідні дані
│   ├── network_n15.csv           # Тестові мережі
│   ├── network_n20.csv
│   └── ...
├── models/
│   ├── element.py                # Класи елементів (Center, Terminal, Consumer)
│   └── network.py                # Клас LogisticsNetwork
├── services/
│   ├── data_loader.py            # Завантаження CSV
│   ├── distance.py               # Обчислення відстаней
│   ├── cost_calculator.py        # Розрахунок витрат
│   ├── visualization.py          # Генерація графіків
│   └── export.py                 # Експорт результатів у CSV
├── optimizers/
│   ├── base.py                   # Базовий клас оптимізатора
│   ├── coordinate.py             # МПО (метод покоординатного спуску)
│   └── genetic.py                # ЕМ-ГА (генетичний алгоритм)
├── utils/
│   ├── network_generator.py      # Генератор тестових мереж
│   └── batch_test.py             # Пакетне тестування
├── results/                      # Згенеровані графіки та CSV
├── main.py                       # Головний файл
├── requirements.txt              # Залежності
├── README.md                     # Цей файл
├── USER_GUIDE.md                 # Посібник користувача
└── TECHNICAL_DOCS.md             # Технічна документація
```

## 📝 Формат вхідних даних

CSV файл з колонками:
- `id` - унікальний ідентифікатор
- `x`, `y` - координати
- `type` - тип елемента (`center`, `terminal`, `consumer`)
- `demand` - попит (для споживачів)
- `terminal_cost` - фіксована вартість терміналу
- `processing_cost` - вартість обробки одиниці товару

Приклад:
```csv
id,x,y,type,demand,terminal_cost,processing_cost
0,50,50,center,0,0,0
10,20,20,terminal,0,8000,15
11,80,80,terminal,0,8000,15
100,20,20,consumer,100,0,0
101,80,80,consumer,85,0,0
```

## 🔬 Алгоритми

### МПО (Метод покоординатного спуску)

**Характеристики:**
- ✅ Детермінований (однакові результати)
- ✅ Точний локальний пошук
- ✅ Гарантована збіжність
- ⚠️ Може застрягти в локальних мінімумах
- ⏱️ Повільніший на великих мережах (n > 30)

**Використання:**
```python
optimizer = CoordinateOptimizer(
    network=network,
    max_iterations=100,
    tolerance=0.1
)
results = optimizer.optimize(verbose=True)
```

### ЕМ-ГА (Генетичний алгоритм)

**Характеристики:**
- ✅ Глобальний пошук
- ✅ Швидкий на великих мережах
- ✅ Уникає локальних мінімумів
- ⚠️ Стохастичний (різні результати)
- 🎯 Кращі рішення на складних топологіях

**Використання:**
```python
optimizer = GeneticOptimizer(
    network=network,
    population_size=100,
    generations=50,
    mutation_rate=0.1,
    crossover_rate=0.8
)
results = optimizer.optimize(verbose=True)
```

## 🛠️ Налаштування

### Параметри МПО

```python
optimizer = CoordinateOptimizer(
    network=network,
    max_iterations=100,    # Максимум проходів
    tolerance=0.1          # Мінімальне покращення (%)
)
```

### Параметри ЕМ-ГА

```python
optimizer = GeneticOptimizer(
    network=network,
    population_size=100,   # Розмір популяції
    generations=50,        # Кількість поколінь
    mutation_rate=0.1,     # Ймовірність мутації
    crossover_rate=0.8     # Ймовірність кросоверу
)
```

## 📈 Експорт результатів

Система автоматично експортує результати в CSV:

**Одиночна оптимізація:** `results/{filename}_optimization_results.csv`
```csv
timestamp,dataset,method,initial_cost,final_cost,improvement_abs,improvement_pct,terminals_before,terminals_after,execution_time,iterations
2025-01-12 10:30:00,network_n15,MPO,66486.31,52600.48,13885.83,20.89,5,3,0.09,2
```

**Порівняння методів:** включає результати обох методів

**Пакетне тестування:** `results/batch_tests/performance_comparison.csv`
- Порівняння МПО та ЕМ-ГА на мережах різних розмірів
- Метрики: час виконання, якість рішення, кількість ітерацій

## 🧪 Тестові мережі

Проект включає 6 автоматично згенерованих тестових мереж:

| Файл | Споживачів | Терміналів | Призначення |
|------|------------|-----------|-------------|
| `network_n15.csv` | 15 | 5 | Мала мережа |
| `network_n20.csv` | 20 | 6 | Середня мережа |
| `network_n25.csv` | 25 | 8 | Середня мережа |
| `network_n30.csv` | 30 | 10 | Велика мережа |
| `network_n35.csv` | 35 | 11 | Велика мережа |
| `network_n40.csv` | 40 | 13 | Дуже велика мережа |

**Типові результати (на сучасному залізі):**

| n | T(МПО) | T(ЕМ-ГА) | δC(ЕМ-ГА) vs МПО |
|---|--------|----------|------------------|
| 15 | 0.08s | 0.18s | -1.7% (кращий) |
| 20 | 0.13s | 0.24s | -3.2% (кращий) |
| 30 | 1.16s | 0.48s | -2.6% (кращий) |
| 40 | 2.36s | 0.68s | -2.9% (кращий) |

## 💡 Рекомендації по вибору методу

### Використовуйте МПО якщо:
- ✅ Потрібен детермінований результат
- ✅ Мережа невелика (< 25 споживачів)
- ✅ Важлива збіжність до локального оптимуму
- ✅ Не критична швидкість виконання

### Використовуйте ЕМ-ГА якщо:
- ✅ Велика мережа (> 30 споживачів)
- ✅ Потрібна швидкість оптимізації
- ✅ Складна топологія з множинними кластерами
- ✅ Важливо уникнути локальних мінімумів

### Використовуйте режим порівняння якщо:
- ✅ Потрібно знайти найкращий метод для вашої мережі
- ✅ Аналіз для дипломної роботи / дослідження
- ✅ Необхідна валідація результатів

## 📖 Документація

- **README.md** (цей файл) - Огляд та швидкий старт
- **USER_GUIDE.md** - Детальний посібник користувача
- **TECHNICAL_DOCS.md** - Технічна документація для розробників

## 🔍 Часті питання

### Q: Чому ЕМ-ГА знаходить кращі рішення ніж МПО?

**A:** МПО виконує локальний пошук і може застрягти в локальному мінімумі. ЕМ-ГА досліджує простір рішень глобально завдяки популяції та генетичним операторам, тому частіше знаходить кращі рішення.

### Q: Результати відрізняються при кожному запуску ЕМ-ГА?

**A:** Так, генетичний алгоритм стохастичний. Для відтворюваності можна встановити seed:
```python
import random
random.seed(42)
```

### Q: Як інтерпретувати негативний δC в порівнянні?

**A:** Негативний δC означає, що ЕМ-ГА знайшов рішення з нижчою вартістю ніж МПО. Це позитивний результат - ЕМ-ГА краще оптимізував мережу.

## 👨‍💻 Автор

Проект розроблено як MVP для оптимізації логістичних мереж з підтримкою множинних алгоритмів оптимізації та автоматичного порівняння методів.

## 📄 Ліцензія

MIT License

---

**Версія:** 1.0
**Дата випуску:** 2025-01-12
**Підтримка:** GitHub Issues

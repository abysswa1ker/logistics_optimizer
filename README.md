# Оптимізація логістичної мережі - MVP

Система оптимізації логістичної мережі з використанням методу покоординатного спуску (МПО).

## 🚀 Можливості

- ✅ Завантаження даних з CSV файлів
- ✅ Обчислення витрат мережі (фіксовані, обробка, транспортування)
- ✅ Оптимізація координат терміналів
- ✅ Автоматичне вимикання невигідних терміналів
- ✅ Візуалізація мережі до/після оптимізації
- ✅ Графіки порівняння витрат

## 📋 Вимоги

- Python 3.8+
- matplotlib

## 🔧 Встановлення

### 1. Клонування репозиторію

```bash
git clone https://github.com/abysswa1ker/logistics_optimizer.git
cd logistics_optimizer
```

### 2. Встановлення залежностей

**Windows:**
```bash
pip install -r requirements.txt
```

**Linux/Mac:**
```bash
pip3 install -r requirements.txt
```

Або встановіть вручну:
```bash
pip install matplotlib
```

## 🎯 Використання

### Запуск оптимізації

```bash
python main.py
```

Програма:
1. Завантажить дані з `data/network_data.csv`
2. Виведе початковий стан мережі та витрати
3. Виконає оптимізацію МПО
4. Покаже результати та економію
5. Згенерує графіки в `results/`

### Результати

Після запуску в директорії `results/` створяться:
- `network_comparison.png` - порівняння мережі до/після
- `cost_comparison.png` - порівняння витрат

## 📊 Приклад результатів

```
Початкові витрати:     76,043.51
Фінальні витрати:      46,049.55
────────────────────────────────
Економія:              29,993.96 (39.44%)
```

**Оптимізації:**
- Вимкнено 2 з 3 терміналів
- 1 термінал переміщено в оптимальну позицію
- Зменшено транспортні витрати на 35%

## 📂 Структура проекту

```
logistics_optimizer/
├── data/
│   └── network_data.csv          # Вхідні дані
├── models/
│   ├── element.py                # Класи елементів (Center, Terminal, Consumer)
│   └── network.py                # Клас LogisticsNetwork
├── services/
│   ├── data_loader.py            # Завантаження CSV
│   ├── distance.py               # Обчислення відстаней
│   ├── cost_calculator.py        # Розрахунок витрат
│   └── visualization.py          # Генерація графіків
├── optimizers/
│   ├── base.py                   # Базовий клас оптимізатора
│   └── coordinate.py             # МПО (метод покоординатного спуску)
├── results/                      # Згенеровані графіки
├── main.py                       # Головний файл
└── requirements.txt              # Залежності
```

## 📝 Формат вхідних даних

CSV файл з колонками:
- `id` - унікальний ідентифікатор
- `x`, `y` - координати
- `type` - тип елемента (`center`, `terminal`, `consumer`)
- `demand` - попит (для споживачів)
- `terminal_cost` - фіксована вартість терміналу
- `processing_cost` - вартість обробки одиниці товару

Приклад:
```csv
id,x,y,type,demand,terminal_cost,processing_cost
0,50,50,center,0,0,0
1,20,30,terminal,0,5000,10
2,80,40,terminal,0,4500,12
4,10,20,consumer,100,0,0
```

## 🔬 Алгоритм МПО

Метод покоординатного спуску:
1. Ітеративно змінює координати кожного терміналу
2. Перевіряє 4 напрямки руху (вгору, вниз, вліво, вправо)
3. Зберігає позицію з найменшими витратами
4. Перевіряє можливість вимкнення терміналів
5. Повторює до збіжності або максимуму ітерацій

## 🛠️ Налаштування

Параметри оптимізації в `main.py`:

```python
optimizer = CoordinateOptimizer(
    network=network,
    step_size=2.0,           # Розмір кроку
    max_iterations=50,       # Максимум ітерацій
    tolerance=0.1            # Мінімальне покращення (%)
)
```

## 📈 Розширення

Готово для додавання:
- Генетичний алгоритм (`optimizers/genetic.py`)
- Додаткові обмеження (капацитети, зони)
- Більше метрик (час доставки, надійність)
- Web-інтерфейс

## 👨‍💻 Автор

Проект розроблено як MVP для оптимізації логістичних мереж.

## 📄 Ліцензія

MIT License
